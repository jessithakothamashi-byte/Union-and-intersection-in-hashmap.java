# Union-and-intersection-in-hashmap.java
Demonstrates the union operation using addAll(). The result contains elements from both sets without duplicates. the intersection operation using retainAll(). Only common elements are retained.
